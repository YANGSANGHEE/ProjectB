import { useCallback, useEffect, useState, useRef } from 'react'
const { kakao } = window; //불러오기에 문제없음
// import vslData from "../testData/vsl.json" //api 복사한 json데이터
import Refresh from '@/Common/Refresh';
import StylePopup from './Enforcement';
import enfCamData from "../testData/unmanned_enforcement_cam_dajeon.json"

const InfoContentStyle = {
  // const displaySet =(qr:string, display:string, flexDirection:string, alignItems:string) =>{
  //   let getQr:any = document.querySelectorAll<HTMLElement>(qr);
  //   getQr.style.display = display;
  //   getQr.style.flexDirection = flexDirection;
  //   getQr.style.alignItems = alignItems;
  // }
}
// bgColor: ,
// borderSet: ,
// font: ,

export default InfoContentStyle;